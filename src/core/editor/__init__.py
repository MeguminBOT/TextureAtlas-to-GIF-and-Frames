"""Editor subpackage for building and manipulating composite animations.

This package provides utilities that let users assemble new animations by
combining frames from existing source animations with optional per-frame
overrides (e.g., custom durations).

Modules:
    editor_composite: Functions for cloning animation maps and constructing
        composite frame sequences from user-defined definitions.
"""
